Netflix Conductor Docker Composition for PDOK
=============================================
A `docker-compose.yml` to set up Netflix Conductor with separate containers for the UI, the server, and redis and elasticsearch backends. 
